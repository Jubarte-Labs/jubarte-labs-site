def word_count(text):
    """
    Counts the number of words in a given text.
    """
    if not text:
        return 0
def word_count(text):
    """
    Counts the number of words in a given text.
    """
    return len(text.split()) if text else 0